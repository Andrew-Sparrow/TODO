import React from 'react';

const style = {
  textAlign: 'center',
  marginBlockStart: 0,
};

const Header = () => (
  <header>
    <h1 style={style}>Task Tracker</h1>
  </header>
);

export default Header;
